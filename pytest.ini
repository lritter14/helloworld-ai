[pytest]
# Configure pytest for the evaluation framework
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

# Ignore warnings about TestResult class in storage.py (it's a dataclass, not a test)
filterwarnings =
    ignore::pytest.PytestCollectionWarning

